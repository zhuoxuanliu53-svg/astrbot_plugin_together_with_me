# together with me 开发文档

## 目标

在同一个群聊内，把容易沉没的“找人联机”消息变成可检索、可限额、可报名的条目。插件不接入任何游戏 API，也不要求游戏内好友关系；“主体文本”可以是好友码、玩家 ID、房间码、邀请链接或服务器地址。

## 条目模型

| 字段 | 规则 |
| --- | --- |
| 主体文本 | 必填；插件原样保存和展示。 |
| 游戏或平台 | 必填；作为检索字段。 |
| 备注 | 可选；自由自然语言，作为检索字段。 |
| 初始人数 | 可选，默认 `1`；创建者可在已经有人组队时设置为更高值。 |
| 人数上限 | 可选，默认 `∞`；报名成功时总人数递增，达到上限后拒绝报名。 |
| 生命周期 | 可选，默认短期；短期 24 小时后删除，长期不自动删除。 |
| 群内识别码 | Bot 自动分配，例如 `TW0001`；只在创建它的群有效。 |

人数总数 = `初始人数 + 成功报名人数`。初始人数不是报名记录，因此创建者不可对自己的条目再报名。

## 命令协议

### 创建

```text
/togethercreate <主体文本>｜<游戏或平台>｜备注=<文本>｜初始=<数字>｜上限=<数字或∞>｜长期
```

只用位置参数会在可选字段被省略时产生歧义，所以协议固定前两段为位置参数，剩余字段以名称标识。可选字段顺序任意。

接受 ASCII `|` 和全角 `｜`；`长期`/`长` 代表长期，`短期`/`短` 代表短期。

### 搜索和列出

```text
/togetherfind [块｜块/替代词]
```

- 空参数列出本群全部未过期条目。
- 每个 `｜` 块必须匹配。
- 同一个块以 `/` 分隔时，任一替代词匹配即可。
- 匹配范围仅为游戏/平台和备注，按大小写无关的子串匹配；不维护强制关键词字段。

### 报名和删除

```text
/togetherwith #TW0001
/togetherdelete #TW0001
```

报名是 Bot 内部登记，不表示已被游戏服务器实际拉入。报名操作用 SQLite 事务执行，确保最后一个名额不会被并发重复占用。删除操作仅允许创建者。

## 存储和生命周期

数据库位于 `data/plugin_data/astrbot_plugin_together_with_me/together_with_me.db`。其数据以群 ID 隔离；识别码按群分别递增。短期条目在每个命令操作前检查清理，并由后台任务每小时清理一次，因此即使没有新消息也会自动删除。

部署到 Docker 时必须持久化 AstrBot 的 `data/` 卷，否则数据库会随容器重建丢失。

## 当前范围

- 已实现：创建、搜索/全量展示、报名计数、创建者删除、短期自动过期、长期保存。
- 未实现：撤销报名、编辑条目、管理员强制删除、通知、跨群查询、游戏账号或邀请码真实性校验。
